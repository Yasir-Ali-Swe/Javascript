console.log("Hell Js");
console.log("let: Declares a block-scoped variable that can be reassigned.");
console.log(
  "const: Declares a block-scoped variable that cannot be reassigned after its initial assignment."
);
console.log(
  "var: Declares a function-scoped or globally-scoped variable that can be reassigned and is hoisted"
);

let num = 123;
console.log("type of Num is " + typeof num);

let text = "123";
console.log("type of text is " + typeof text);

let isAdult = true;
console.log("type of isAdult is " + typeof isAdult);

let list = [1, 2, 3, "Yasir Ali"];
console.log("Type of list is " + typeof list);

let obj = {
  name: "Yasir Ali",
  Gender: "Male",
};
console.log("Type of obj is " + typeof obj);
